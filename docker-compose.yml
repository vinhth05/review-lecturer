services:
  # 1. Redis
  redis:
    image: redis:alpine
    container_name: lecture-redis
    ports:
      - "6379:6379"
    restart: always
    volumes:
      - redis-data:/data

  # 2. Zookeeper
  zookeeper:
    image: wurstmeister/zookeeper:latest
    container_name: zookeeper
    ports:
      - "2181:2181"

  # 3. Kafka
  kafka-broker:
    image: wurstmeister/kafka:latest
    container_name: kafka-broker
    ports:
      - "9092:9092"
    environment:
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_CREATE_TOPICS: "otp-email-topic:1:1"
    depends_on:
      - zookeeper

  # 4. SQL Server
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: lecture-sqlserver
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "yourStrong(!)Password"
    ports:
      - "1433:1433"
    volumes:
      - sqlserver-data:/var/opt/mssql

#   # 5. Backend Spring Boot
#   backend:
#     build:
#       context: ./backend
#       dockerfile: Dockerfile
#     container_name: lecture-backend
#     ports:
#       - "8080:8080"
#     environment:
#       - DB_URL=jdbc:sqlserver://sqlserver:1433;databaseName=ctu_review;encrypt=true;trustServerCertificate=true
#       - REDIS_HOST=redis
#       - KAFKA_BOOTSTRAP_SERVERS=kafka-broker:9092
#     depends_on:
#       - sqlserver
#       - redis
#       - kafka-broker
# 
#   # 6. Frontend React Vite (Nginx)
#   frontend:
#     build:
#       context: ./frontend
#       dockerfile: Dockerfile
#     container_name: lecture-frontend-prod
#     ports:
#       - "80:80"
#     environment:
#       - VITE_API_BASE_URL=http://localhost:8080
#     depends_on:
#       - backend

volumes:
  sqlserver-data:
  redis-data:
